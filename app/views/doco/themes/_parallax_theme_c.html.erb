<script>
    var ids = [];
    $("form[action='/cart/add']").each(function(i, e) {
        ids.push($(e).attr('id').split('-')[2]);
    });

    var bta = {
        product_id: ids.join(',')
    }
</script>