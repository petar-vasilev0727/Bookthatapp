<h1>Listing waitlists</h1>

<table class="styled">
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @waitlists.each do |waitlist| %>
  <tr>
    <td><%= link_to 'Show', product_waitlists_path(@product, waitlist) %></td>
    <td><%= link_to 'Edit', edit_product_waitlist_path(@product, waitlist) %></td>
    <td><%= link_to 'Destroy', [@product, waitlist], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Waitlist', new_product_waitlist_path(@product) %>
