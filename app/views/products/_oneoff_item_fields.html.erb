<tr class="fields">
  <%= f.hidden_field :id %>
  <td><%= f.text_field :start, :required => 'required', :class=> 'datetimepicker', :size => '18', :value => datetime(f.object.start) %></td>
  <td><%= f.link_to_remove "Remove" %></td>
</tr>
